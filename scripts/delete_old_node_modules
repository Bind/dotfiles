#!/bin/bash

set -euo pipefail

echo "Finding node_modules directories older than 120 days..."

# Use -print0 and xargs -0 for safer handling of paths with spaces
find . -name "node_modules" -type d -mtime +120 -print0 | xargs -0 -r rm -rf

echo "Cleanup complete!"